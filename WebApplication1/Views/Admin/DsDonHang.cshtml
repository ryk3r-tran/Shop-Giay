@using KarmaModels.KarmaModels
@using WebApplication1.Models
@using PagedList.Mvc
@model PagedList.PagedList<DonHangAD>
<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<table class="table table-borderless datatable">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Khách hàng</th>
            <th scope="col">Sản phẩm</th>
            <th scope="col">Giá trị</th>
            <th scope="col">Tình trạng</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var d in Model)
        {
            <tr>
                <th scope="row"><a href="#">@d.MaDH</a></th>
                <td>@d.TenKH</td>
                <td><a href="#" class="text-primary">@d.TenSP</a></td>
                <td>@d.TongTien</td>
                <td><span class="badge bg-success">@d.TinhTrang</span></td>
            </tr>
        }

    </tbody>
</table>

<div class="pageIndexs">

    @for (int i = 1; i <= Model.PageCount; i++)
    {


        if (i == Model.PageNumber)
        {

            <span data-id="@i" style="color:orangered" class="pageIndex">@i</span>
        }
        else
        {
            <span data-id="@i" style="color:black" class="pageIndex">@i</span>
        }

    }



    <script>
        $(document).ready(function () {
            $('.pageIndex').click(function () {
                var ChiSoTrang = $(this).data('id');
                LoadDsDH(ChiSoTrang);
            })
        })


    </script>
    <style>
        .pageIndexs {
            padding: 20px 0 0 50px;
        }

            .pageIndexs span {
                margin-left: 10px;
                padding: 10px;
                background-color: azure;
            }

                .pageIndexs span:hover {
                    cursor: pointer;
                }
    </style>

</div>

@*@Html.PagedListPager(BinhLuan, page => Url.Action("SanPhamChiTiet", new { page }))*@

